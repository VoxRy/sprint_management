<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <record id="project_sprint_create_wizard_view_form" model="ir.ui.view">
    <field name="name">project.sprint.create.wizard.form</field>
    <field name="model">project.sprint.create.wizard</field>
    <field name="arch" type="xml">
      <form string="Create Sprint">
          <group>
            <group>
              <field name="project_id" readonly="1"/>
              <field name="name"/>
              <field name="duration" widget="selection"/>
            </group>
            <group>
              <field name="start_date"/>
              <field name="end_date" attrs="{'readonly': [('duration', '!=', 'custom')], 'required': True}"/>
            </group>
          </group>

          <group string="Sprint goal" colspan="4">
            <field name="goal" nolabel="1" placeholder="What do we want to achieve in this sprint?"/>
          </group>

          <group string="Select Backlog Tasks" colspan="4">
            <field name="task_ids" nolabel="1" context="{'default_project_id': project_id}">
              <tree>
                <field name="priority" widget="priority"/>
                <field name="name"/>
                <field name="epic_id"/>
                <field name="user_ids" widget="many2many_tags"/>
                <field name="stage_id"/>
              </tree>
            </field>
          </group>

        <footer>
          <button string="Create Sprint" name="action_create_sprint" type="object" class="btn-primary"/>
          <button string="Cancel" class="btn-secondary" special="cancel"/>
        </footer>
      </form>
    </field>
  </record>

</odoo>
